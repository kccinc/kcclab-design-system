import { Meta } from "@storybook/addon-docs/blocks";
import { radius } from "@kccinc/kcclab-design-system/tokens";

<Meta title="Foundation/Border Radius" />

# Border Radius

`packages/ui/src/styles/tokens.css`

`--ds-radius-*`로 정의되어 있으며, `rounded-[var(--ds-radius-sm)]` 형태로 사용하세요.

export const RadiusSwatch = ({ name, value }) => (
  <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 8 }}>
    <div
      style={{
        width: 72,
        height: 72,
        borderRadius: value,
        background: "#e8f3ff",
        border: "1px solid #90c2ff",
      }}
    />
    <div style={{ fontFamily: "monospace", fontSize: 13, textAlign: "center" }}>
      <div>radius-{name}</div>
      <div style={{ color: "#6b7684" }}>{value}</div>
    </div>
  </div>
);

<div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(100px, 1fr))", gap: 24 }}>
  {Object.entries(radius).map(([name, value]) => (
    <RadiusSwatch key={name} name={name} value={value} />
  ))}
</div>

/** Focus ring (keyboard accessibility) tokens — mirrors `--ds-ring-*` in tokens.css. */
export const focusRing = {
  color: "#3182f6", // same as colors.action.primary.DEFAULT
  width: "2px",
  offsetWidth: "2px",
} as const;
